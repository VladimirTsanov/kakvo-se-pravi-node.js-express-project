<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <%- include('header') %>

        <main>
            <br>
            <section> All articles in the category: <% allCategories.forEach((e)=> { %>
                    <% if (e.slug===slug) { %>
                        <%= e.name %>
                            <% }}) %>
            </section> <br>

            <% list_of_TheCategory.forEach(article=> { %>
                <section>
                    <a href="/article/<%= article.slug %>"><h3><%= article.title %></h3></a>
                    <a href="/article/<%= article.slug %>"><h3><%- article.content %></h3></a>
                </section>

            <% }); %>
        </main>

        <%- include('footer') %>
</body>

</html>